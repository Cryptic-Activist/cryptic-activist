import { describe, expect, test } from 'vitest';
import { createChat } from '../../src/functions';

describe('Chat tests', () => {
  test('it should create a new chat', async () => {
    // const newChat = await createChat({tradeId: ''})
    // console.log({ newBlock });
  });
});
