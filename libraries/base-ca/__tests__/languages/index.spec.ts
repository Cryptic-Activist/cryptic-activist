import { describe, expect, test } from 'vitest';
import { createLanguage } from '../../src/functions/languages';
import { Language } from '../../src/services/prisma';

describe('Language tests', () => {
  test('it should create a new language', async () => {
    // const newLanguage = await createLanguage({ name: 'English' });
    // console.log({ newLanguage });
  });
});
