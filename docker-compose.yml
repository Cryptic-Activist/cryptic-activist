version: "3"

services:
  user-api:
    build:
      context: api-user-cryptic-activist/
      dockerfile: Dockerfile
    ports:
      - "5005:5005"
    env_file:
      - api-user-cryptic-activist/development.env
