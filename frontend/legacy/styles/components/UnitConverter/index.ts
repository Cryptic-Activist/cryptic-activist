import styled from "styled-components";

export const Converter = styled.span`
	font-size: 15px;
	font-weight: 900;
	letter-spacing: 0.02rem;
	color: ${({ theme }) => theme.components.search.offers.form.info.button.color};
`;
